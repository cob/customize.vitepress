export interface TextualContent {
    filepaths: string[],
    videos: string[],
    instance: number,
    path: string,
    localeCode: string
    title: string
    content: string
    restrictions: string[]
}